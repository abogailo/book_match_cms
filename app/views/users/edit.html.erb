

<%= form_for @user do |f| %>
    <%= f.label :admin %>
    <%= f.text_field :admin %><br>
  
    <%= f.submit 'Update User' %>
  <% end %>

  <%= link_to("Delete", user_path(@user), method: :delete, :class => 'action delete') unless !current_user%>
